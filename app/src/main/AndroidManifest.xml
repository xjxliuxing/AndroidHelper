<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.xjx.helper">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".global.CommonBaseApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning"
        tools:targetApi="q">
        <activity android:name=".ui.home.activity.tod.CustomViewActivity"></activity>
        <activity android:name=".ui.home.activity.tod.CoortorLayoutActivity" />
        <activity android:name=".ui.home.activity.tod.NetWorkCurrentKbActivity" />
        <activity android:name=".ui.home.activity.tod.GridLayoutLayout2Activity" />
        <activity android:name=".ui.home.activity.tod.Patch9Activity" />
        <activity android:name=".ui.home.activity.tod.DrawableTextViewActivity" />
        <activity android:name=".ui.home.activity.tod.GridLayoutLayoutActivity" />
        <activity android:name=".test.TestActivity" />
        <activity android:name=".ui.home.activity.tod.PlayerActivity" />
        <activity android:name=".ui.home.activity.tod.RecycleViewDivederActivity" />
        <activity android:name=".ui.home.activity.tod.TestActivity" />
        <activity android:name=".ui.home.activity.tod.CustomTime2Activity" />
        <activity android:name=".ui.home.activity.tod.CustomTimeActivity" />
        <activity android:name=".ui.DownLoadManagerActivity" />
        <activity android:name=".ui.home.activity.tod.DownLoadActivity" /> <!-- 主界面 -->
        <activity android:name=".ui.home.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.FileProvider"
            android:exported="false"
            android:grantUriPermissions="true"
            android:label="7.0临时访问目录权限的处理">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>